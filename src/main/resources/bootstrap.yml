spring:
  application:
    name: ventas
  profiles:
    active: localhost
  cloud:
    config:
      username: root
      password: s2cr3t
      uri: https://xrodrig.dnsnet.info:8445/fuxpin-config-server/
      name: ventas
  config:
    import: configserver:https://xrodrig.dnsnet.info:8445/fuxpin-config-server/