spring:
  profiles:
    # Profile actived. example: ventas-localhost.yml
    active: localhost
  cloud:
    config:
      # user + password Config Cloud Config Server
      username: root
      password: s2cr3t
      uri: https://xrodrig.dnsnet.info:8445/fuxpin-config-server/
      name: ventas
      # branch: master / develop / feature ...
      label: develop
  config:
    import: configserver:https://xrodrig.dnsnet.info:8445/fuxpin-config-server/